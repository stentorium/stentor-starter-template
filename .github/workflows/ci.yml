name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
      with:
       node-version: '10.18.x'
    - name: Build
      run: yarn build
    - name: Test
      run: yarn test
